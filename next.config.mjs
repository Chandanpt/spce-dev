/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["custom.cvent.com", "img.vggcdn.net", "res.iluv.southwest.com", 'image.o.delta.com'],
  },
};

export default nextConfig;
